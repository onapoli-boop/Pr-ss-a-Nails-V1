<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>
  {{ page_title }}
  {%- if current_tags %} &ndash; {{ 'general.search.tagged' | t: tags: current_tags | join: ', ' }}{% endif -%}
  {%- if current_page != 1 %} &ndash; {{ 'general.meta.page' | t: page: current_page }}{% endif -%}
  {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
</title>
{%- if page_description %}
<meta name="description" content="{{ page_description | escape }}">
{%- endif %}
<link rel="canonical" href="{{ canonical_url }}">
<link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;1,9..144,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
{{ 'theme.css' | asset_url | stylesheet_tag }}

{{ content_for_header }}
</head>
<body class="template-{{ template.name }}">
  <a class="visually-hidden skip-to-content-link" href="#MainContent">{{ 'general.accessibility.skip_to_content' | t }}</a>

  {% section 'announcement-bar' %}
  {% section 'site-header' %}

  <main id="MainContent">
    {{ content_for_layout }}
  </main>

  {% section 'site-footer' %}

  {% render 'cart-drawer' %}

  {%- liquid
    assign seven_days_collection = settings.seven_days_collection
    assign seven_days_variant_ids = blank
    if seven_days_collection
      for product in seven_days_collection.products
        assign variant_ids = product.variants | map: 'id'
        if seven_days_variant_ids == blank
          assign seven_days_variant_ids = variant_ids
        else
          assign seven_days_variant_ids = seven_days_variant_ids | concat: variant_ids
        endif
      endfor
    endif
  -%}
  <script>
    window.theme = window.theme || {};
    window.theme.currency = {{ cart.currency.iso_code | json }};
    window.theme.freeGiftThreshold = {{ settings.free_gift_threshold | json }};
    window.theme.strings = {
      adding: {{ 'cart.adding' | t | json }},
      added: {{ 'cart.added' | t | json }},
      giftNote: {{ 'cart.gift_note' | t | json }},
      giftUnlocked: {{ 'cart.gift_unlocked' | t | json }},
      remove: {{ 'cart.remove' | t | json }},
      decrease: {{ 'cart.decrease' | t | json }},
      increase: {{ 'cart.increase' | t | json }},
      wishlistAdd: {{ 'products.wishlist_add' | t | json }},
      wishlistAdded: {{ 'products.wishlist_added' | t | json }},
      kitProgress: {{ 'seven_days_kit.progress' | t | json }},
      kitComplete: {{ 'seven_days_kit.complete' | t | json }},
      kitAdd: {{ 'seven_days_kit.add_to_kit' | t | json }},
      kitInKit: {{ 'seven_days_kit.in_kit' | t | json }}
    };
    window.theme.sevenDaysKit = {
      variantIds: {{ seven_days_variant_ids | json }},
      target: {{ settings.seven_days_target | default: 7 | json }},
      price: {{ settings.seven_days_price | json }}
    };
  </script>

  {%- comment -%}
    GSAP + ScrollTrigger from the mockup, kept as-is. assets/theme.js
    detects window.gsap and falls back to plain JS/CSS if these CDN
    scripts fail to load (offline, blocked, etc.) — see the hasGSAP
    pattern at the top of the file.
  {%- endcomment -%}
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>
  <script src="{{ 'theme.js' | asset_url }}" defer="defer"></script>
</body>
</html>
